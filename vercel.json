{
  "version": 3,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "dist/server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/dist/server/server.js" }
  ]
}
